// Archivo generado por scripts/embed-readme.mjs — no editar a mano.
// Fuente: README.md

/** Contenido de README.md en Markdown */
export const README_MARKDOWN: string = "# Backend API — Jaramillo Mora (`auth`)\n\nAPI REST con **NestJS 11**, **Prisma 7** (PostgreSQL) y **Redis**. Expone **autenticación JWT**, **carga de archivos IFC** (base64) y un **pipeline de procesamiento IFC/BIM** con [`web-ifc`](https://github.com/ThatOpen/engine_web-ifc) que parsea el modelo y persiste su dominio para alimentar visores 3D y gráficas.\n\n> ℹ️ **Alcance actual:** el proyecto **evolucionó** de un CRUD de `clientes`/`servicios` hacia un **procesador de modelos IFC**. Esos módulos fueron **eliminados** del código; el contexto completo (arquitectura, capas, diagramas) está en [`AGENTS.md`](./AGENTS.md).\n\n| Recurso | Valor |\n|---------|--------|\n| Prefijo API | `/api/v1/` |\n| Swagger UI | `/api-docs` |\n| Storage público (visor 3D) | `/storage/<archivo>` |\n| Contexto para agentes IA / arquitectura | [`AGENTS.md`](./AGENTS.md) |\n| Skills del proyecto | [`.agents/skills/`](./.agents/skills/) |\n| Bitácora de cambios | [`CHANGELOG.md`](./CHANGELOG.md) |\n\n---\n\n## Arquitectura (resumen)\n\nArquitectura por **capas + feature modules + repository pattern** sobre NestJS. Diagramas completos (capas, secuencia IFC, modelo de datos) en [`AGENTS.md`](./AGENTS.md#2-diagrama-de-arquitectura-capas).\n\n```\nCliente (Angular :4200)\n   │ HTTPS api/v1 · GET /storage\n   ▼\nBootstrap HTTP (main.ts): CORS · body limits · ValidationPipe · /storage · Swagger\n   ▼\nTransversal @Global: ResponseInterceptor · GlobalExceptionFilter · JwtAuthGuard/Auth\n                     Redis · AdminFile · IfcProcessing · Prisma\n   ▼\nFeature Modules (controllers):  UsersModule · ProcessIfcModule\n   ▼\nServicios (casos de uso):  AuthService · ProcessIfcService · IfcProcessingService · …\n   ▼\nDominio (tokens abstractos + entities):  *Repository\n   ▼\nInfraestructura:  Prisma*Repository · RedisService · AdminFile(fs) · web-ifc(WASM)\n   ▼\nPostgreSQL 17 · Redis :6390 · Filesystem (src/storage)\n```\n\n---\n\n## Requisitos\n\n- **Node.js** 24.x (alineado con la imagen Docker `node:24.14.1-alpine`)\n- **npm** (incluido con Node)\n- **Docker** + **Docker Compose** (infra y/o API en contenedor)\n- PostgreSQL y Redis: vía Compose (recomendado) o instancias propias\n\nOpcional: crear de antemano las rutas de bind mounts (`POSTGRES_DATA_PATH`, `REDIS_DATA_PATH`, `REDIS_LOGS_PATH`) con permisos de escritura.\n\n---\n\n## Configuración de entorno\n\n```bash\ncp .env.example .env\n# Completar .env (nunca subir secretos al repositorio)\n```\n\n### Variables principales\n\n| Variable | Descripción |\n|----------|-------------|\n| `SERVER_PORT` | Puerto HTTP de la API (Compose y `.env.example`: `3050`) |\n| `HOST` | Host anunciado en logs (ej. `localhost`) |\n| `BODY_LIMIT` | Límite del body JSON/urlencoded para base64 IFC/PDF (ej. `100mb`) |\n| `CORS_ORIGINS` | Orígenes CORS permitidos, coma-separados (incluye `http://localhost:4200`) |\n| `DB_HOST`, `DB_PORT`, `DB_DATABASE`, `DB_USER`, `DB_PASSWORD` | Credenciales Postgres |\n| `DATABASE_URL` | URL Prisma, ej. `postgres://USER:PASS@localhost:5453/DB?schema=public` |\n| `POSTGRES_DATA_PATH` | Ruta absoluta en el host para el volumen de datos Postgres (Compose) |\n| `DURATION_TOKEN` | Caducidad JWT (ej. `15d`) |\n| `SECRECT_KEY` | Secreto JWT (**nombre con typo histórico; no renombrar sin migrar envs**) |\n| `REDIS_HOST` / `REDIS_HOST_DOCKER` | `localhost` en dev local; en Compose la API usa `redis-cache-jm` |\n| `REDIS_PORT` | Debe ser **6390** (coincide con `redis/redis.conf`) |\n| `REDIS_PASSWORD` | Password Redis |\n| `REDIS_DB`, `REDIS_SESSION_DB`, `REDIS_CACHE_DB` | Índices lógicos Redis |\n| `REDIS_TTL`, `REDIS_MAX_ITEMS` | Caché Nest |\n| `REDIS_DATA_PATH`, `REDIS_LOGS_PATH` | Bind mounts Redis |\n| `REDIS_COMMANDER_PORT` | UI Redis Commander (profile `tools`) |\n| `REDIS_MAXMEMORY`, `REDIS_MEMORY_*` | Límites de memoria Redis/Compose |\n\nEl archivo tipado de lectura es `src/core/config/envs.ts`. Si añades una variable nueva, decláralas ahí y en `.env.example`.\n\n> **Notas clave**\n> - Redis escucha en **6390** (`redis/redis.conf`). El healthcheck de Compose usa ese puerto; `redis-cli` por defecto usa `6379` y marcaría el contenedor como *unhealthy*.\n> - Con `docker compose`, la API sobrescribe `DB_HOST` / `DATABASE_URL` / `REDIS_HOST` / `SERVER_PORT` para hablar con los servicios de la red interna (no uses `localhost` del host dentro del contenedor).\n\n---\n\n## Arranque en desarrollo\n\n### Opción A — Infra en Docker + API en el host (recomendada para coding)\n\n```bash\n# 1. Variables\ncp .env.example .env\n# API local: REDIS_HOST=localhost, REDIS_PORT=6390\n# DATABASE_URL apuntando a localhost:5453\n\n# 2. Postgres + Redis\ndocker compose up -d postgres-db-jm redis-cache-jm\n\n# 3. Dependencias y cliente Prisma\nnpm install            # 'postinstall' ejecuta 'prisma generate'\nnpx prisma generate\n\n# 4. (Una vez) crear funciones SQL de agregación IFC — ver sección \"Funciones SQL IFC\"\n\n# 5. API con watch\nnpm run start:dev\n```\n\n- API: `http://localhost:<SERVER_PORT>` (según `.env`)\n- Swagger: `http://localhost:<SERVER_PORT>/api-docs`\n- Storage: `http://localhost:<SERVER_PORT>/storage`\n- Postgres host: `localhost:5453` · Redis host: `localhost:6390`\n\n### Opción B — Stack completo con Compose (API en contenedor)\n\n```bash\ncp .env.example .env\n# Ajustar POSTGRES_DATA_PATH y secretos\n\ndocker compose up -d --build\n```\n\n| Servicio | Contenedor | Puerto host |\n|----------|------------|-------------|\n| Postgres | `postgres-db-jm` | `5453` → `5432` |\n| Redis | `redis-cache-jm` | `6390` → `6390` |\n| API | `api-auth-jm` | `3050` → `3050` |\n\nEl servicio API usa el stage Docker **`development`**, monta el código y ejecuta `start_docker.sh` (install, `prisma generate`, build, **pm2** sobre `dist/src/main.js`).\n\nRedis Commander (opcional):\n\n```bash\ndocker compose --profile tools up -d redis-commander-jm\n# UI: http://localhost:8091 (o REDIS_COMMANDER_PORT)\n```\n\n---\n\n## Funciones SQL IFC (paso obligatorio)\n\nEl endpoint `POST api/v1/process-ifc/process-property-sets-ifc` agrega datos con **funciones de PostgreSQL** invocadas vía `$queryRaw`:\n\n- `public.get_elementbynivel(id_model)`\n- `public.get_elementbycategory(id_model)`\n\nEstas funciones **no** están en `init-scripts/init-db.sql`; deben crearse en la base de datos (referencia de patrones en `db/plsql/`). Sin ellas, ese endpoint falla al construir `elementsDB`. Aplica el SQL una vez tras levantar Postgres, por ejemplo:\n\n```bash\n# Con psql contra la DB local (ajusta credenciales/puerto)\npsql \"postgres://USER:PASS@localhost:5453/DB\" -f db/plsql/<archivo>.sql\n```\n\n---\n\n## Uso rápido de la API\n\n```bash\n# 1. Login (público) → obtener accessToken\ncurl -X POST http://localhost:3050/api/v1/users/auth/login \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"login\":\"usuario\",\"passwd\":\"secreto\"}'\n\n# 2. Subir un IFC en base64 (JWT requerido)\ncurl -X POST http://localhost:3050/api/v1/process-ifc \\\n  -H \"Authorization: Bearer <TOKEN>\" -H \"Content-Type: application/json\" \\\n  -d '{\"nom_file\":\"modelo_edificio\",\"ext\":\"ifc\",\"size\":1024,\"base64\":\"<BASE64>\"}'\n\n# 3. Procesar y obtener datos para graficar (parseo web-ifc + persistencia)\ncurl -X POST http://localhost:3050/api/v1/process-ifc/process-property-sets-ifc \\\n  -H \"Authorization: Bearer <TOKEN>\" -H \"Content-Type: application/json\" \\\n  -d '{\"nom_file\":\"modelo_edificio.ifc\"}'\n\n# 4. Listar archivos IFC cargados (con URL pública /storage)\ncurl http://localhost:3050/api/v1/process-ifc/getFileIfcAll \\\n  -H \"Authorization: Bearer <TOKEN>\"\n```\n\nEndpoints completos: ver Swagger en `/api-docs` o [`AGENTS.md` §5.1](./AGENTS.md#51-endpoints-existentes-resumen).\n\n---\n\n## Scripts npm útiles\n\n| Script | Qué hace |\n|--------|----------|\n| `npm run start:dev` | Limpia y arranca Nest en watch |\n| `npm run start:debug` | Nest debug + watch |\n| `npm run build` | Genera docs embebidas, limpia y compila (`nest build`) |\n| `npm run start:prod` | `node dist/main` (tras `build`) |\n| `npm run prisma:generate:models` | `prisma db pull` + `generate` (reflejar schema desde DB) |\n| `npx prisma generate` | Regenerar cliente sin pull |\n| `npm run lint` | ESLint con fix |\n| `npm run test` / `test:e2e` | Jest unitario / e2e |\n| `npm run docs:build` | Embed de este README en `src/core/docs/readme.generated.ts` |\n\n`postinstall` ejecuta `prisma generate` automáticamente; `prebuild` ejecuta `docs:build`.\n\n---\n\n## Arranque en producción\n\n### 1. Build y ejecución directa\n\n```bash\nnpm ci\nnpx prisma generate\nnpm run build\nNODE_ENV=production npm run start:prod\n```\n\nAsegura que `DATABASE_URL`, `SECRECT_KEY`, `CORS_ORIGINS`, `BODY_LIMIT`, Redis y `SERVER_PORT` estén definidos en el entorno de ejecución (no uses valores de desarrollo).\n\n> El script `start:prod` apunta a `node dist/main`. Verifica en tu `dist/` la ruta real del entrypoint tras el build (`dist/main.js` vs `dist/src/main.js`) y alinea el script o el `CMD` Docker si difieren (el stage `production` del Dockerfile usa `node dist/src/main.js`).\n\n### 2. Imagen Docker multi-stage (API)\n\nDockerfile: `dockerfile/api/Dockerfile`\n\n| Stage | Uso |\n|-------|-----|\n| `development` | Hot path / Compose actual (bind mounts + pm2) |\n| `builder` | `npm run build` + deps de producción |\n| `production` | Imagen final: `node dist/src/main.js`, `EXPOSE 3050` |\n\n```bash\ndocker build -f dockerfile/api/Dockerfile --target production -t api-auth-jm:prod .\n\ndocker run --rm --env-file .env \\\n  -e REDIS_HOST=<host-redis-accesible> \\\n  -e DB_HOST=<host-postgres-accesible> \\\n  -p 3050:3050 \\\n  api-auth-jm:prod\n```\n\n### 3. Consideraciones de producción\n\n1. Construir/usar `--target production` en el servicio API (el Compose actual está orientado a **desarrollo**).\n2. No montar el código fuente como volumen.\n3. Fijar `NODE_ENV=production` y secretos robustos (`SECRECT_KEY`).\n4. Postgres y Redis como servicios gestionados o contenedores con volúmenes persistentes y backups (`backup_db.sh` / carpeta `backups/`).\n5. Aplicar las **funciones SQL IFC** en la DB de producción.\n6. Persistir/backup del directorio `src/storage` (archivos IFC subidos).\n7. Exponer solo los puertos necesarios detrás de un reverse proxy (hay `vhost-auth.conf` como referencia) y revisar límites de memoria Redis y healthchecks.\n\n---\n\n## Modelo de datos (Prisma)\n\nArchivo: `prisma/schema.prisma` (schema SQL `public`). Diagrama ER en [`AGENTS.md` §2.2](./AGENTS.md#22-modelo-de-datos-ifc-prisma-postgresql).\n\n**Dominio IFC (activo):**\n\n- `ref_files` → archivos IFC cargados (nombre + ruta storage)\n- `modelo_ifc` → cabecera del modelo parseado (esquema, app creadora, totales)\n- `agrupacion_ifc` → estructura espacial (sitio, edificio, niveles, espacios, zonas)\n- `elemento_ifc` → elementos contables por agrupación\n- `property_set` → property sets por elemento\n- `propiedad_parametro` → propiedades/parámetros por pset\n- `cantidad_ifc` → cantidades físicas por elemento\n\n**Autenticación:**\n\n- `identity_type`, `users`\n\n**Legacy (tablas sin módulo Nest):** `clientes`, `servicios`, `tipo_servicios` (permanecen por reflejo de la DB; no hay features que las expongan).\n\nFlujo de esquema (reflejo desde la DB, sin `prisma/migrations/`):\n\n```bash\nnpm run prisma:generate:models   # prisma db pull + generate\n```\n\nInicialización de DB en Docker: `init-scripts/init-db.sql` (montado en el entrypoint de Postgres). Recuerda además crear las **funciones SQL IFC** (sección anterior).\n\n---\n\n## Desarrollar nuevos objetos (guía rápida)\n\nSigue el patrón de **`src/modules/process-ifc/`** (o `users/`). Contexto ampliado y checklist detallado: [`AGENTS.md` §8](./AGENTS.md#8-cómo-extender-checklist-para-agentes).\n\n### Feature module\n\n```\nsrc/modules/<feature>/\n  <feature>.module.ts\n  controllers/<usecase>/...\n  controllers/dto/int|out/\n  controllers/mappers/\n  services/<usecase>/\n  repositories/\n  interfaces/          # clase abstracta = token DI\n  entities/\n```\n\n1. **Entity** de dominio → **Interface** abstracta del repositorio (token DI) → **Prisma repository** con `PrismaService`.\n2. **Service** por caso de uso → **DTOs** int/out con `class-validator` + Swagger → **Mapper** entity ↔ DTO.\n3. **Controller** delgado; `@UseGuards(JwtAuthGuard)` + `@ApiBearerAuth('JWT-auth')` salvo endpoints públicos.\n4. Registrar el repositorio: `{ provide: MiRepository, useClass: PrismaMiRepository }` y añadir el módulo a `imports` de `src/app.module.ts`.\n5. Respuestas: devolver `{ datos, mensaje }`; el `ResponseInterceptor` global arma `ResponseGeneralDto`.\n6. Caché (opcional): inyectar `RedisService` (`src/core/redis/`, ya global).\n\n### Nueva tabla del dominio IFC\n\nCrea su micro-módulo `<tabla>-ifc.module.ts` (token + Prisma impl), impórtalo en `IfcProcessingModule`, inyecta el repo en `IfcProcessingService` y añade su paso idempotente. Detalle en [`AGENTS.md` §8](./AGENTS.md#8-cómo-extender-checklist-para-agentes).\n\n### Calidad y convenciones\n\n```bash\nnpm run lint\nnpm run test\n```\n\n- Convenciones Nest obligatorias: skill **nestjs-best-practices** en `.agents/skills/nestjs-best-practices/`.\n- Commits (cuando se pidan): skills **commiter** + **changelog-manager** (Conventional Commits + `CHANGELOG.md`).\n\n---\n\n## Docker — resumen de archivos\n\n| Archivo | Rol |\n|---------|-----|\n| `docker-compose.yml` | Orquestación Postgres, Redis, API, Redis Commander (`red_jaramillomora`) |\n| `dockerfile/api/Dockerfile` | Stages development / builder / production |\n| `dockerfile/db/Dockerfile` | Imagen Postgres + init |\n| `start_docker.sh` | Entrypoint contenedor API (dev, pm2) |\n| `redis/redis.conf` | Redis en puerto 6390, persistencia RDB+AOF |\n\n---\n\n## Solución de problemas\n\n| Problema | Qué revisar |\n|----------|-------------|\n| API no conecta a Redis | `REDIS_HOST` / `REDIS_PORT=6390` / password; en Compose el hostname es `redis-cache-jm` |\n| Prisma no genera cliente | `DATABASE_URL` válida; `npx prisma generate` |\n| Postgres no inicia | `POSTGRES_DATA_PATH` existe y es escribible; logs del contenedor |\n| `process-property-sets-ifc` falla | Faltan funciones SQL `get_elementbynivel` / `get_elementbycategory` en la DB |\n| Error base64 / body demasiado grande | Ajustar `BODY_LIMIT` (ej. `100mb`) |\n| Fetch a `/storage` bloqueado (CORS) | Añadir el origen del frontend a `CORS_ORIGINS` |\n| JWT inválido | `SECRECT_KEY` y `DURATION_TOKEN` iguales en todos los entornos que firman/verifican |\n| Puerto ocupado | Liberar `SERVER_PORT` / `3050` / `5453` / `6390` |\n| Create user \"no existe\" en API | `CreateUserController` no está registrado en `UsersModule` (deuda conocida; ver `AGENTS.md`) |\n\n---\n\n## Licencia\n\nProyecto privado (`UNLICENSED`).\n";

/** README.md convertido a HTML (marked) para Swagger u otras vistas */
export const README_HTML: string = "<h1>Backend API — Jaramillo Mora (<code>auth</code>)</h1>\n<p>API REST con <strong>NestJS 11</strong>, <strong>Prisma 7</strong> (PostgreSQL) y <strong>Redis</strong>. Expone <strong>autenticación JWT</strong>, <strong>carga de archivos IFC</strong> (base64) y un <strong>pipeline de procesamiento IFC/BIM</strong> con <a href=\"https://github.com/ThatOpen/engine_web-ifc\"><code>web-ifc</code></a> que parsea el modelo y persiste su dominio para alimentar visores 3D y gráficas.</p>\n<blockquote>\n<p>ℹ️ <strong>Alcance actual:</strong> el proyecto <strong>evolucionó</strong> de un CRUD de <code>clientes</code>/<code>servicios</code> hacia un <strong>procesador de modelos IFC</strong>. Esos módulos fueron <strong>eliminados</strong> del código; el contexto completo (arquitectura, capas, diagramas) está en <a href=\"./AGENTS.md\"><code>AGENTS.md</code></a>.</p>\n</blockquote>\n<table>\n<thead>\n<tr>\n<th>Recurso</th>\n<th>Valor</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Prefijo API</td>\n<td><code>/api/v1/</code></td>\n</tr>\n<tr>\n<td>Swagger UI</td>\n<td><code>/api-docs</code></td>\n</tr>\n<tr>\n<td>Storage público (visor 3D)</td>\n<td><code>/storage/&lt;archivo&gt;</code></td>\n</tr>\n<tr>\n<td>Contexto para agentes IA / arquitectura</td>\n<td><a href=\"./AGENTS.md\"><code>AGENTS.md</code></a></td>\n</tr>\n<tr>\n<td>Skills del proyecto</td>\n<td><a href=\"./.agents/skills/\"><code>.agents/skills/</code></a></td>\n</tr>\n<tr>\n<td>Bitácora de cambios</td>\n<td><a href=\"./CHANGELOG.md\"><code>CHANGELOG.md</code></a></td>\n</tr>\n</tbody></table>\n<hr>\n<h2>Arquitectura (resumen)</h2>\n<p>Arquitectura por <strong>capas + feature modules + repository pattern</strong> sobre NestJS. Diagramas completos (capas, secuencia IFC, modelo de datos) en <a href=\"./AGENTS.md#2-diagrama-de-arquitectura-capas\"><code>AGENTS.md</code></a>.</p>\n<pre><code>Cliente (Angular :4200)\n   │ HTTPS api/v1 · GET /storage\n   ▼\nBootstrap HTTP (main.ts): CORS · body limits · ValidationPipe · /storage · Swagger\n   ▼\nTransversal @Global: ResponseInterceptor · GlobalExceptionFilter · JwtAuthGuard/Auth\n                     Redis · AdminFile · IfcProcessing · Prisma\n   ▼\nFeature Modules (controllers):  UsersModule · ProcessIfcModule\n   ▼\nServicios (casos de uso):  AuthService · ProcessIfcService · IfcProcessingService · …\n   ▼\nDominio (tokens abstractos + entities):  *Repository\n   ▼\nInfraestructura:  Prisma*Repository · RedisService · AdminFile(fs) · web-ifc(WASM)\n   ▼\nPostgreSQL 17 · Redis :6390 · Filesystem (src/storage)\n</code></pre>\n<hr>\n<h2>Requisitos</h2>\n<ul>\n<li><strong>Node.js</strong> 24.x (alineado con la imagen Docker <code>node:24.14.1-alpine</code>)</li>\n<li><strong>npm</strong> (incluido con Node)</li>\n<li><strong>Docker</strong> + <strong>Docker Compose</strong> (infra y/o API en contenedor)</li>\n<li>PostgreSQL y Redis: vía Compose (recomendado) o instancias propias</li>\n</ul>\n<p>Opcional: crear de antemano las rutas de bind mounts (<code>POSTGRES_DATA_PATH</code>, <code>REDIS_DATA_PATH</code>, <code>REDIS_LOGS_PATH</code>) con permisos de escritura.</p>\n<hr>\n<h2>Configuración de entorno</h2>\n<pre><code class=\"language-bash\">cp .env.example .env\n# Completar .env (nunca subir secretos al repositorio)\n</code></pre>\n<h3>Variables principales</h3>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Descripción</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>SERVER_PORT</code></td>\n<td>Puerto HTTP de la API (Compose y <code>.env.example</code>: <code>3050</code>)</td>\n</tr>\n<tr>\n<td><code>HOST</code></td>\n<td>Host anunciado en logs (ej. <code>localhost</code>)</td>\n</tr>\n<tr>\n<td><code>BODY_LIMIT</code></td>\n<td>Límite del body JSON/urlencoded para base64 IFC/PDF (ej. <code>100mb</code>)</td>\n</tr>\n<tr>\n<td><code>CORS_ORIGINS</code></td>\n<td>Orígenes CORS permitidos, coma-separados (incluye <code>http://localhost:4200</code>)</td>\n</tr>\n<tr>\n<td><code>DB_HOST</code>, <code>DB_PORT</code>, <code>DB_DATABASE</code>, <code>DB_USER</code>, <code>DB_PASSWORD</code></td>\n<td>Credenciales Postgres</td>\n</tr>\n<tr>\n<td><code>DATABASE_URL</code></td>\n<td>URL Prisma, ej. <code>postgres://USER:PASS@localhost:5453/DB?schema=public</code></td>\n</tr>\n<tr>\n<td><code>POSTGRES_DATA_PATH</code></td>\n<td>Ruta absoluta en el host para el volumen de datos Postgres (Compose)</td>\n</tr>\n<tr>\n<td><code>DURATION_TOKEN</code></td>\n<td>Caducidad JWT (ej. <code>15d</code>)</td>\n</tr>\n<tr>\n<td><code>SECRECT_KEY</code></td>\n<td>Secreto JWT (<strong>nombre con typo histórico; no renombrar sin migrar envs</strong>)</td>\n</tr>\n<tr>\n<td><code>REDIS_HOST</code> / <code>REDIS_HOST_DOCKER</code></td>\n<td><code>localhost</code> en dev local; en Compose la API usa <code>redis-cache-jm</code></td>\n</tr>\n<tr>\n<td><code>REDIS_PORT</code></td>\n<td>Debe ser <strong>6390</strong> (coincide con <code>redis/redis.conf</code>)</td>\n</tr>\n<tr>\n<td><code>REDIS_PASSWORD</code></td>\n<td>Password Redis</td>\n</tr>\n<tr>\n<td><code>REDIS_DB</code>, <code>REDIS_SESSION_DB</code>, <code>REDIS_CACHE_DB</code></td>\n<td>Índices lógicos Redis</td>\n</tr>\n<tr>\n<td><code>REDIS_TTL</code>, <code>REDIS_MAX_ITEMS</code></td>\n<td>Caché Nest</td>\n</tr>\n<tr>\n<td><code>REDIS_DATA_PATH</code>, <code>REDIS_LOGS_PATH</code></td>\n<td>Bind mounts Redis</td>\n</tr>\n<tr>\n<td><code>REDIS_COMMANDER_PORT</code></td>\n<td>UI Redis Commander (profile <code>tools</code>)</td>\n</tr>\n<tr>\n<td><code>REDIS_MAXMEMORY</code>, <code>REDIS_MEMORY_*</code></td>\n<td>Límites de memoria Redis/Compose</td>\n</tr>\n</tbody></table>\n<p>El archivo tipado de lectura es <code>src/core/config/envs.ts</code>. Si añades una variable nueva, decláralas ahí y en <code>.env.example</code>.</p>\n<blockquote>\n<p><strong>Notas clave</strong></p>\n<ul>\n<li>Redis escucha en <strong>6390</strong> (<code>redis/redis.conf</code>). El healthcheck de Compose usa ese puerto; <code>redis-cli</code> por defecto usa <code>6379</code> y marcaría el contenedor como <em>unhealthy</em>.</li>\n<li>Con <code>docker compose</code>, la API sobrescribe <code>DB_HOST</code> / <code>DATABASE_URL</code> / <code>REDIS_HOST</code> / <code>SERVER_PORT</code> para hablar con los servicios de la red interna (no uses <code>localhost</code> del host dentro del contenedor).</li>\n</ul>\n</blockquote>\n<hr>\n<h2>Arranque en desarrollo</h2>\n<h3>Opción A — Infra en Docker + API en el host (recomendada para coding)</h3>\n<pre><code class=\"language-bash\"># 1. Variables\ncp .env.example .env\n# API local: REDIS_HOST=localhost, REDIS_PORT=6390\n# DATABASE_URL apuntando a localhost:5453\n\n# 2. Postgres + Redis\ndocker compose up -d postgres-db-jm redis-cache-jm\n\n# 3. Dependencias y cliente Prisma\nnpm install            # &#39;postinstall&#39; ejecuta &#39;prisma generate&#39;\nnpx prisma generate\n\n# 4. (Una vez) crear funciones SQL de agregación IFC — ver sección &quot;Funciones SQL IFC&quot;\n\n# 5. API con watch\nnpm run start:dev\n</code></pre>\n<ul>\n<li>API: <code>http://localhost:&lt;SERVER_PORT&gt;</code> (según <code>.env</code>)</li>\n<li>Swagger: <code>http://localhost:&lt;SERVER_PORT&gt;/api-docs</code></li>\n<li>Storage: <code>http://localhost:&lt;SERVER_PORT&gt;/storage</code></li>\n<li>Postgres host: <code>localhost:5453</code> · Redis host: <code>localhost:6390</code></li>\n</ul>\n<h3>Opción B — Stack completo con Compose (API en contenedor)</h3>\n<pre><code class=\"language-bash\">cp .env.example .env\n# Ajustar POSTGRES_DATA_PATH y secretos\n\ndocker compose up -d --build\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Servicio</th>\n<th>Contenedor</th>\n<th>Puerto host</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Postgres</td>\n<td><code>postgres-db-jm</code></td>\n<td><code>5453</code> → <code>5432</code></td>\n</tr>\n<tr>\n<td>Redis</td>\n<td><code>redis-cache-jm</code></td>\n<td><code>6390</code> → <code>6390</code></td>\n</tr>\n<tr>\n<td>API</td>\n<td><code>api-auth-jm</code></td>\n<td><code>3050</code> → <code>3050</code></td>\n</tr>\n</tbody></table>\n<p>El servicio API usa el stage Docker <strong><code>development</code></strong>, monta el código y ejecuta <code>start_docker.sh</code> (install, <code>prisma generate</code>, build, <strong>pm2</strong> sobre <code>dist/src/main.js</code>).</p>\n<p>Redis Commander (opcional):</p>\n<pre><code class=\"language-bash\">docker compose --profile tools up -d redis-commander-jm\n# UI: http://localhost:8091 (o REDIS_COMMANDER_PORT)\n</code></pre>\n<hr>\n<h2>Funciones SQL IFC (paso obligatorio)</h2>\n<p>El endpoint <code>POST api/v1/process-ifc/process-property-sets-ifc</code> agrega datos con <strong>funciones de PostgreSQL</strong> invocadas vía <code>$queryRaw</code>:</p>\n<ul>\n<li><code>public.get_elementbynivel(id_model)</code></li>\n<li><code>public.get_elementbycategory(id_model)</code></li>\n</ul>\n<p>Estas funciones <strong>no</strong> están en <code>init-scripts/init-db.sql</code>; deben crearse en la base de datos (referencia de patrones en <code>db/plsql/</code>). Sin ellas, ese endpoint falla al construir <code>elementsDB</code>. Aplica el SQL una vez tras levantar Postgres, por ejemplo:</p>\n<pre><code class=\"language-bash\"># Con psql contra la DB local (ajusta credenciales/puerto)\npsql &quot;postgres://USER:PASS@localhost:5453/DB&quot; -f db/plsql/&lt;archivo&gt;.sql\n</code></pre>\n<hr>\n<h2>Uso rápido de la API</h2>\n<pre><code class=\"language-bash\"># 1. Login (público) → obtener accessToken\ncurl -X POST http://localhost:3050/api/v1/users/auth/login \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d &#39;{&quot;login&quot;:&quot;usuario&quot;,&quot;passwd&quot;:&quot;secreto&quot;}&#39;\n\n# 2. Subir un IFC en base64 (JWT requerido)\ncurl -X POST http://localhost:3050/api/v1/process-ifc \\\n  -H &quot;Authorization: Bearer &lt;TOKEN&gt;&quot; -H &quot;Content-Type: application/json&quot; \\\n  -d &#39;{&quot;nom_file&quot;:&quot;modelo_edificio&quot;,&quot;ext&quot;:&quot;ifc&quot;,&quot;size&quot;:1024,&quot;base64&quot;:&quot;&lt;BASE64&gt;&quot;}&#39;\n\n# 3. Procesar y obtener datos para graficar (parseo web-ifc + persistencia)\ncurl -X POST http://localhost:3050/api/v1/process-ifc/process-property-sets-ifc \\\n  -H &quot;Authorization: Bearer &lt;TOKEN&gt;&quot; -H &quot;Content-Type: application/json&quot; \\\n  -d &#39;{&quot;nom_file&quot;:&quot;modelo_edificio.ifc&quot;}&#39;\n\n# 4. Listar archivos IFC cargados (con URL pública /storage)\ncurl http://localhost:3050/api/v1/process-ifc/getFileIfcAll \\\n  -H &quot;Authorization: Bearer &lt;TOKEN&gt;&quot;\n</code></pre>\n<p>Endpoints completos: ver Swagger en <code>/api-docs</code> o <a href=\"./AGENTS.md#51-endpoints-existentes-resumen\"><code>AGENTS.md</code> §5.1</a>.</p>\n<hr>\n<h2>Scripts npm útiles</h2>\n<table>\n<thead>\n<tr>\n<th>Script</th>\n<th>Qué hace</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>npm run start:dev</code></td>\n<td>Limpia y arranca Nest en watch</td>\n</tr>\n<tr>\n<td><code>npm run start:debug</code></td>\n<td>Nest debug + watch</td>\n</tr>\n<tr>\n<td><code>npm run build</code></td>\n<td>Genera docs embebidas, limpia y compila (<code>nest build</code>)</td>\n</tr>\n<tr>\n<td><code>npm run start:prod</code></td>\n<td><code>node dist/main</code> (tras <code>build</code>)</td>\n</tr>\n<tr>\n<td><code>npm run prisma:generate:models</code></td>\n<td><code>prisma db pull</code> + <code>generate</code> (reflejar schema desde DB)</td>\n</tr>\n<tr>\n<td><code>npx prisma generate</code></td>\n<td>Regenerar cliente sin pull</td>\n</tr>\n<tr>\n<td><code>npm run lint</code></td>\n<td>ESLint con fix</td>\n</tr>\n<tr>\n<td><code>npm run test</code> / <code>test:e2e</code></td>\n<td>Jest unitario / e2e</td>\n</tr>\n<tr>\n<td><code>npm run docs:build</code></td>\n<td>Embed de este README en <code>src/core/docs/readme.generated.ts</code></td>\n</tr>\n</tbody></table>\n<p><code>postinstall</code> ejecuta <code>prisma generate</code> automáticamente; <code>prebuild</code> ejecuta <code>docs:build</code>.</p>\n<hr>\n<h2>Arranque en producción</h2>\n<h3>1. Build y ejecución directa</h3>\n<pre><code class=\"language-bash\">npm ci\nnpx prisma generate\nnpm run build\nNODE_ENV=production npm run start:prod\n</code></pre>\n<p>Asegura que <code>DATABASE_URL</code>, <code>SECRECT_KEY</code>, <code>CORS_ORIGINS</code>, <code>BODY_LIMIT</code>, Redis y <code>SERVER_PORT</code> estén definidos en el entorno de ejecución (no uses valores de desarrollo).</p>\n<blockquote>\n<p>El script <code>start:prod</code> apunta a <code>node dist/main</code>. Verifica en tu <code>dist/</code> la ruta real del entrypoint tras el build (<code>dist/main.js</code> vs <code>dist/src/main.js</code>) y alinea el script o el <code>CMD</code> Docker si difieren (el stage <code>production</code> del Dockerfile usa <code>node dist/src/main.js</code>).</p>\n</blockquote>\n<h3>2. Imagen Docker multi-stage (API)</h3>\n<p>Dockerfile: <code>dockerfile/api/Dockerfile</code></p>\n<table>\n<thead>\n<tr>\n<th>Stage</th>\n<th>Uso</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>development</code></td>\n<td>Hot path / Compose actual (bind mounts + pm2)</td>\n</tr>\n<tr>\n<td><code>builder</code></td>\n<td><code>npm run build</code> + deps de producción</td>\n</tr>\n<tr>\n<td><code>production</code></td>\n<td>Imagen final: <code>node dist/src/main.js</code>, <code>EXPOSE 3050</code></td>\n</tr>\n</tbody></table>\n<pre><code class=\"language-bash\">docker build -f dockerfile/api/Dockerfile --target production -t api-auth-jm:prod .\n\ndocker run --rm --env-file .env \\\n  -e REDIS_HOST=&lt;host-redis-accesible&gt; \\\n  -e DB_HOST=&lt;host-postgres-accesible&gt; \\\n  -p 3050:3050 \\\n  api-auth-jm:prod\n</code></pre>\n<h3>3. Consideraciones de producción</h3>\n<ol>\n<li>Construir/usar <code>--target production</code> en el servicio API (el Compose actual está orientado a <strong>desarrollo</strong>).</li>\n<li>No montar el código fuente como volumen.</li>\n<li>Fijar <code>NODE_ENV=production</code> y secretos robustos (<code>SECRECT_KEY</code>).</li>\n<li>Postgres y Redis como servicios gestionados o contenedores con volúmenes persistentes y backups (<code>backup_db.sh</code> / carpeta <code>backups/</code>).</li>\n<li>Aplicar las <strong>funciones SQL IFC</strong> en la DB de producción.</li>\n<li>Persistir/backup del directorio <code>src/storage</code> (archivos IFC subidos).</li>\n<li>Exponer solo los puertos necesarios detrás de un reverse proxy (hay <code>vhost-auth.conf</code> como referencia) y revisar límites de memoria Redis y healthchecks.</li>\n</ol>\n<hr>\n<h2>Modelo de datos (Prisma)</h2>\n<p>Archivo: <code>prisma/schema.prisma</code> (schema SQL <code>public</code>). Diagrama ER en <a href=\"./AGENTS.md#22-modelo-de-datos-ifc-prisma-postgresql\"><code>AGENTS.md</code> §2.2</a>.</p>\n<p><strong>Dominio IFC (activo):</strong></p>\n<ul>\n<li><code>ref_files</code> → archivos IFC cargados (nombre + ruta storage)</li>\n<li><code>modelo_ifc</code> → cabecera del modelo parseado (esquema, app creadora, totales)</li>\n<li><code>agrupacion_ifc</code> → estructura espacial (sitio, edificio, niveles, espacios, zonas)</li>\n<li><code>elemento_ifc</code> → elementos contables por agrupación</li>\n<li><code>property_set</code> → property sets por elemento</li>\n<li><code>propiedad_parametro</code> → propiedades/parámetros por pset</li>\n<li><code>cantidad_ifc</code> → cantidades físicas por elemento</li>\n</ul>\n<p><strong>Autenticación:</strong></p>\n<ul>\n<li><code>identity_type</code>, <code>users</code></li>\n</ul>\n<p><strong>Legacy (tablas sin módulo Nest):</strong> <code>clientes</code>, <code>servicios</code>, <code>tipo_servicios</code> (permanecen por reflejo de la DB; no hay features que las expongan).</p>\n<p>Flujo de esquema (reflejo desde la DB, sin <code>prisma/migrations/</code>):</p>\n<pre><code class=\"language-bash\">npm run prisma:generate:models   # prisma db pull + generate\n</code></pre>\n<p>Inicialización de DB en Docker: <code>init-scripts/init-db.sql</code> (montado en el entrypoint de Postgres). Recuerda además crear las <strong>funciones SQL IFC</strong> (sección anterior).</p>\n<hr>\n<h2>Desarrollar nuevos objetos (guía rápida)</h2>\n<p>Sigue el patrón de <strong><code>src/modules/process-ifc/</code></strong> (o <code>users/</code>). Contexto ampliado y checklist detallado: <a href=\"./AGENTS.md#8-c%C3%B3mo-extender-checklist-para-agentes\"><code>AGENTS.md</code> §8</a>.</p>\n<h3>Feature module</h3>\n<pre><code>src/modules/&lt;feature&gt;/\n  &lt;feature&gt;.module.ts\n  controllers/&lt;usecase&gt;/...\n  controllers/dto/int|out/\n  controllers/mappers/\n  services/&lt;usecase&gt;/\n  repositories/\n  interfaces/          # clase abstracta = token DI\n  entities/\n</code></pre>\n<ol>\n<li><strong>Entity</strong> de dominio → <strong>Interface</strong> abstracta del repositorio (token DI) → <strong>Prisma repository</strong> con <code>PrismaService</code>.</li>\n<li><strong>Service</strong> por caso de uso → <strong>DTOs</strong> int/out con <code>class-validator</code> + Swagger → <strong>Mapper</strong> entity ↔ DTO.</li>\n<li><strong>Controller</strong> delgado; <code>@UseGuards(JwtAuthGuard)</code> + <code>@ApiBearerAuth(&#39;JWT-auth&#39;)</code> salvo endpoints públicos.</li>\n<li>Registrar el repositorio: <code>{ provide: MiRepository, useClass: PrismaMiRepository }</code> y añadir el módulo a <code>imports</code> de <code>src/app.module.ts</code>.</li>\n<li>Respuestas: devolver <code>{ datos, mensaje }</code>; el <code>ResponseInterceptor</code> global arma <code>ResponseGeneralDto</code>.</li>\n<li>Caché (opcional): inyectar <code>RedisService</code> (<code>src/core/redis/</code>, ya global).</li>\n</ol>\n<h3>Nueva tabla del dominio IFC</h3>\n<p>Crea su micro-módulo <code>&lt;tabla&gt;-ifc.module.ts</code> (token + Prisma impl), impórtalo en <code>IfcProcessingModule</code>, inyecta el repo en <code>IfcProcessingService</code> y añade su paso idempotente. Detalle en <a href=\"./AGENTS.md#8-c%C3%B3mo-extender-checklist-para-agentes\"><code>AGENTS.md</code> §8</a>.</p>\n<h3>Calidad y convenciones</h3>\n<pre><code class=\"language-bash\">npm run lint\nnpm run test\n</code></pre>\n<ul>\n<li>Convenciones Nest obligatorias: skill <strong>nestjs-best-practices</strong> en <code>.agents/skills/nestjs-best-practices/</code>.</li>\n<li>Commits (cuando se pidan): skills <strong>commiter</strong> + <strong>changelog-manager</strong> (Conventional Commits + <code>CHANGELOG.md</code>).</li>\n</ul>\n<hr>\n<h2>Docker — resumen de archivos</h2>\n<table>\n<thead>\n<tr>\n<th>Archivo</th>\n<th>Rol</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>docker-compose.yml</code></td>\n<td>Orquestación Postgres, Redis, API, Redis Commander (<code>red_jaramillomora</code>)</td>\n</tr>\n<tr>\n<td><code>dockerfile/api/Dockerfile</code></td>\n<td>Stages development / builder / production</td>\n</tr>\n<tr>\n<td><code>dockerfile/db/Dockerfile</code></td>\n<td>Imagen Postgres + init</td>\n</tr>\n<tr>\n<td><code>start_docker.sh</code></td>\n<td>Entrypoint contenedor API (dev, pm2)</td>\n</tr>\n<tr>\n<td><code>redis/redis.conf</code></td>\n<td>Redis en puerto 6390, persistencia RDB+AOF</td>\n</tr>\n</tbody></table>\n<hr>\n<h2>Solución de problemas</h2>\n<table>\n<thead>\n<tr>\n<th>Problema</th>\n<th>Qué revisar</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>API no conecta a Redis</td>\n<td><code>REDIS_HOST</code> / <code>REDIS_PORT=6390</code> / password; en Compose el hostname es <code>redis-cache-jm</code></td>\n</tr>\n<tr>\n<td>Prisma no genera cliente</td>\n<td><code>DATABASE_URL</code> válida; <code>npx prisma generate</code></td>\n</tr>\n<tr>\n<td>Postgres no inicia</td>\n<td><code>POSTGRES_DATA_PATH</code> existe y es escribible; logs del contenedor</td>\n</tr>\n<tr>\n<td><code>process-property-sets-ifc</code> falla</td>\n<td>Faltan funciones SQL <code>get_elementbynivel</code> / <code>get_elementbycategory</code> en la DB</td>\n</tr>\n<tr>\n<td>Error base64 / body demasiado grande</td>\n<td>Ajustar <code>BODY_LIMIT</code> (ej. <code>100mb</code>)</td>\n</tr>\n<tr>\n<td>Fetch a <code>/storage</code> bloqueado (CORS)</td>\n<td>Añadir el origen del frontend a <code>CORS_ORIGINS</code></td>\n</tr>\n<tr>\n<td>JWT inválido</td>\n<td><code>SECRECT_KEY</code> y <code>DURATION_TOKEN</code> iguales en todos los entornos que firman/verifican</td>\n</tr>\n<tr>\n<td>Puerto ocupado</td>\n<td>Liberar <code>SERVER_PORT</code> / <code>3050</code> / <code>5453</code> / <code>6390</code></td>\n</tr>\n<tr>\n<td>Create user &quot;no existe&quot; en API</td>\n<td><code>CreateUserController</code> no está registrado en <code>UsersModule</code> (deuda conocida; ver <code>AGENTS.md</code>)</td>\n</tr>\n</tbody></table>\n<hr>\n<h2>Licencia</h2>\n<p>Proyecto privado (<code>UNLICENSED</code>).</p>\n";
