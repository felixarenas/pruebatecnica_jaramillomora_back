// Archivo generado por scripts/embed-readme.mjs — no editar a mano.
// Fuente: README.md

/** Contenido de README.md en Markdown */
export const README_MARKDOWN: string = "# Backend API — Jaramillo Mora (`auth`)\n\nAPI REST con **NestJS 11**, **Prisma 7** (PostgreSQL) y **Redis**. Expone autenticación JWT y CRUD de usuarios, clientes y servicios.\n\n| Recurso | Valor |\n|---------|--------|\n| Prefijo API | `/api/v1/` |\n| Swagger UI | `/api-docs` |\n| Contexto para agentes IA | [`AGENTS.md`](./AGENTS.md) |\n| Skills del proyecto | [`.agents/skills/`](./.agents/skills/) |\n\n---\n\n## Requisitos\n\n- **Node.js** 24.x (alineado con la imagen Docker `node:24.14.1-alpine`)\n- **npm** (incluido con Node)\n- **Docker** + **Docker Compose** (infra y/o API en contenedor)\n- PostgreSQL y Redis: vía Compose (recomendado) o instancias propias\n\nOpcional: crear de antemano las rutas de bind mounts de datos (`POSTGRES_DATA_PATH`, `REDIS_DATA_PATH`, `REDIS_LOGS_PATH`) con permisos de escritura.\n\n---\n\n## Configuración de entorno\n\n1. Copiar la plantilla:\n\n```bash\ncp .env.example .env\n```\n\n2. Completar `.env` (nunca subir secretos al repositorio).\n\n### Variables principales\n\n| Variable | Descripción |\n|----------|-------------|\n| `SERVER_PORT` | Puerto HTTP de la API (local tipico `3001`; Compose mapea `3050`) |\n| `HOST` | Host anunciado en logs (ej. `localhost`) |\n| `DB_HOST`, `DB_PORT`, `DB_DATABASE`, `DB_USER`, `DB_PASSWORD` | Credenciales Postgres |\n| `DATABASE_URL` | URL Prisma, ej. `postgres://USER:PASS@HOST:PORT/DB?schema=public` |\n| `POSTGRES_DATA_PATH` | Ruta absoluta en el host para el volumen de datos Postgres (Compose) |\n| `DURATION_TOKEN` | Caducidad JWT (ej. `15d`) |\n| `SECRECT_KEY` | Secreto JWT (**nombre con typo histórico; no renombrar sin migrar envs**) |\n| `REDIS_HOST` | `localhost` en desarrollo local; en Compose la API usa `redis-cache-jm` |\n| `REDIS_PORT` | Debe ser **6390** (coincide con `redis/redis.conf`) |\n| `REDIS_PASSWORD` | Password Redis |\n| `REDIS_DB`, `REDIS_SESSION_DB`, `REDIS_CACHE_DB` | Índices lógicos Redis |\n| `REDIS_TTL`, `REDIS_MAX_ITEMS` | Caché Nest |\n| `REDIS_DATA_PATH`, `REDIS_LOGS_PATH` | Bind mounts Redis |\n| `REDIS_COMMANDER_PORT` | UI Redis Commander (profile `tools`) |\n| `REDIS_MAXMEMORY`, `REDIS_MEMORY_*` | Límites de memoria Redis/Compose |\n\nEl archivo tipado de lectura es `src/core/config/envs.ts`. Si añades una variable nueva, declárala ahí y en `.env.example`.\n\n> **Nota:** en este proyecto Redis escucha en **6390** (`redis/redis.conf`). El healthcheck de Compose debe usar ese puerto; `redis-cli` por defecto usa `6379` y marcaría el contenedor como *unhealthy*.\n>\n> Con `docker compose`, la API sobrescribe `DB_HOST`/`DATABASE_URL`/`REDIS_HOST`/`SERVER_PORT` para hablar con los servicios de la red interna (no uses `localhost` del host dentro del contenedor).\n\n---\n\n## Arranque en desarrollo\n\n### Opción A — Solo infra en Docker + API en el host (recomendada para coding)\n\n```bash\n# 1. Variables\ncp .env.example .env\n# Editar .env: DB_*, DATABASE_URL, REDIS_*, SECRECT_KEY, POSTGRES_DATA_PATH, etc.\n# Para API local: REDIS_HOST=localhost, REDIS_PORT=6390\n# DATABASE_URL apuntando a localhost:5453\n\n# 2. Postgres + Redis\ndocker compose up -d postgres-db-jm redis-cache-jm\n\n# 3. Dependencias y cliente Prisma\nnpm install\nnpx prisma generate\n\n# 4. API con watch\nnpm run start:dev\n```\n\n- API: `http://localhost:<SERVER_PORT>` (según `.env`)\n- Swagger: `http://localhost:<SERVER_PORT>/api-docs`\n- Postgres host: `localhost:5453`\n- Redis host: `localhost:6390`\n\n### Opción B — Stack completo con Compose (API en contenedor)\n\n```bash\ncp .env.example .env\n# Ajustar paths de volúmenes y secretos\n# SERVER_PORT dentro del contenedor debe coincidir con el mapeo (Compose usa 3050:3050)\n\ndocker compose up -d --build\n```\n\nServicios:\n\n| Servicio | Contenedor | Puerto host |\n|----------|------------|-------------|\n| Postgres | `postgres-db-jm` | `5453` → `5432` |\n| Redis | `redis-cache-jm` | `6390` → `6390` |\n| API | `api-auth-jm` | `3050` → `3050` |\n\nEl servicio API usa el stage Docker **`development`**, monta el código y ejecuta `start_docker.sh` (install, `prisma generate`, build, **pm2**).\n\nRedis Commander (opcional):\n\n```bash\ndocker compose --profile tools up -d redis-commander-jm\n```\n\nUI por defecto: `http://localhost:8091` (o `REDIS_COMMANDER_PORT`).\n\n### Scripts npm útiles\n\n| Script | Qué hace |\n|--------|----------|\n| `npm run start:dev` | Limpia y arranca Nest en watch |\n| `npm run start:debug` | Nest debug + watch |\n| `npm run build` | Genera docs embebidas, limpia y compila (`nest build`) |\n| `npm run start:prod` | `node dist/main` (tras `build`) |\n| `npm run prisma:generate:models` | `prisma db pull` + `generate` (reflejar schema desde DB) |\n| `npx prisma generate` | Regenerar cliente sin pull |\n| `npm run lint` | ESLint con fix |\n| `npm run test` / `test:e2e` | Jest unitario / e2e |\n| `npm run docs:build` | Embed de este README en `src/core/docs/readme.generated.ts` |\n\n`postinstall` ejecuta `prisma generate` automáticamente.\n\n---\n\n## Arranque en producción\n\n### 1. Build de la aplicación\n\n```bash\nnpm ci\nnpx prisma generate\nnpm run build\nNODE_ENV=production npm run start:prod\n```\n\nAsegura que `DATABASE_URL`, `SECRECT_KEY`, Redis y `SERVER_PORT` estén definidos en el entorno de ejecución (no uses valores de desarrollo).\n\n> El script `start:prod` apunta a `node dist/main`. Verifica en tu `dist/` la ruta real del entrypoint tras el build (`dist/main.js` vs `dist/src/main.js`) y alinea el script o el `CMD` Docker si difieren.\n\n### 2. Imagen Docker multi-stage (API)\n\nDockerfile: `dockerfile/api/Dockerfile`\n\n| Stage | Uso |\n|-------|-----|\n| `development` | Hot path / Compose actual |\n| `builder` | `npm run build` + deps de producción |\n| `production` | Imagen final: `node dist/src/main.js`, `EXPOSE 3050` |\n\nEjemplo de build y ejecución del stage de producción:\n\n```bash\ndocker build -f dockerfile/api/Dockerfile --target production -t api-auth-jm:prod .\n\ndocker run --rm --env-file .env \\\n  -e REDIS_HOST=<host-redis-accesible> \\\n  -p 3050:3050 \\\n  api-auth-jm:prod\n```\n\nPostgres y Redis en producción deben ser servicios gestionados o contenedores con volúmenes persistentes, backups (`backup_db.sh` / carpeta `backups/`) y secretos fuera del código.\n\n### 3. Compose orientado a producción\n\nEl `docker-compose.yml` actual está pensado sobre todo para **desarrollo** (`target: development`, bind mounts, pm2). Para producción:\n\n1. Construir/usar `--target production` en el servicio API.\n2. No montar el código fuente como volumen.\n3. Fijar `NODE_ENV=production` y secretos robustos.\n4. Revisar límites de memoria Redis y healthchecks.\n5. Exponer solo los puertos necesarios detrás de un reverse proxy (hay `vhost-auth.conf` como referencia).\n\n---\n\n## Modelo de datos (Prisma)\n\nArchivo: `prisma/schema.prisma` (schema SQL `public`).\n\n- `identity_type` → tipos de identificación\n- `users` → usuarios del sistema (login)\n- `clientes` → clientes de negocio\n- `servicios` / `tipo_servicios` → servicios contratados y catálogo\n\nRelaciones: tipos de id compartidos entre `users` y `clientes`; cada `servicio` pertenece a un `cliente` y a un `tipo_servicios`.\n\nInicialización de DB en Docker: `init-scripts/init-db.sql` (montado en el entrypoint de Postgres).\n\nSi el esquema cambia en Postgres:\n\n```bash\nnpm run prisma:generate:models\n```\n\n---\n\n## Desarrollar nuevos objetos (guía rápida)\n\nSigue el patrón de **`src/modules/clientes/`** (o `servicios/`). Contexto ampliado: [`AGENTS.md`](./AGENTS.md).\n\n### 1. Crear el feature module\n\n```\nsrc/modules/<feature>/\n  <feature>.module.ts\n  controllers/<usecase>/...\n  controllers/dto/int|out/\n  controllers/mappers/\n  services/<usecase>/\n  repositories/\n  interfaces/\n  entities/\n```\n\n### 2. Capas\n\n1. **Entity** de dominio.\n2. **Interface** abstracta del repositorio (token DI).\n3. **Prisma repository** con `PrismaService`.\n4. **Service** por caso de uso (crear, buscar, actualizar…).\n5. **DTOs** de entrada/salida con `class-validator` y decoradores Swagger.\n6. **Mapper** entity ↔ DTO.\n7. **Controller**: delgado; `@UseGuards(JwtAuthGuard)` y `@ApiBearerAuth` si aplica.\n\n### 3. Registrar\n\nEn `<feature>.module.ts`:\n\n```typescript\n{ provide: MiRepository, useClass: PrismaMiRepository }\n```\n\nEn `src/app.module.ts`, añadir el módulo a `imports`.\n\n### 4. Auth y respuestas\n\n- Importar `AuthModule` desde `src/core/config/auth.module/`.\n- Devolver datos; el `ResponseInterceptor` global arma la respuesta estándar.\n- Opcional: `@ResponseMessage('...')`.\n\n### 5. Caché (opcional)\n\nInyectar `RedisService` (`src/core/redis/`) — el módulo ya es global.\n\n### 6. Calidad\n\n```bash\nnpm run lint\nnpm run test\n```\n\nConvenciones Nest obligatorias del equipo: skill **nestjs-best-practices** en `.agents/skills/nestjs-best-practices/`.\n\nCommits (cuando se pidan): skills **commiter** + **changelog-manager** (Conventional Commits + `CHANGELOG.md`).\n\n---\n\n## Módulos de negocio actuales\n\n| Módulo | Ruta | Responsabilidad |\n|--------|------|-----------------|\n| Users | `src/modules/users/` | Login JWT, consulta/actualización de usuarios |\n| Clientes | `src/modules/clientes/` | CRUD clientes y tipos de identificación |\n| Servicios | `src/modules/servicios/` | CRUD servicios, tipos y consultas por cliente |\n\nInfra transversal: `PrismaModule`, `RedisModule`, `AuthModule`, filter e interceptor globales.\n\n---\n\n## Docker — resumen de archivos\n\n| Archivo | Rol |\n|---------|-----|\n| `docker-compose.yml` | Orquestación Postgres, Redis, API, Commander |\n| `dockerfile/api/Dockerfile` | Stages development / builder / production |\n| `dockerfile/db/Dockerfile` | Imagen Postgres + init |\n| `start_docker.sh` | Entrypoint contenedor API (dev) |\n| `redis/redis.conf` | Redis en puerto 6390, persistencia RDB+AOF |\n\nRed Compose: `red_jaramillomora`.\n\n---\n\n## Solución de problemas\n\n| Problema | Qué revisar |\n|----------|-------------|\n| API no conecta a Redis | `REDIS_HOST` / `REDIS_PORT=6390` / password; en Compose el hostname es `redis-cache-jm` |\n| Prisma no genera cliente | `DATABASE_URL` válida; `npx prisma generate` |\n| Postgres no inicia | `POSTGRES_DATA_PATH` existe y es escribible; logs del contenedor |\n| JWT inválido | `SECRECT_KEY` y `DURATION_TOKEN` iguales en todos los entornos que firman/verifican |\n| Puerto ocupado | Liberar `SERVER_PORT` / `3050` / `5453` / `6390` |\n| Create user “no existe” en API | El controller de creación no está registrado en `UsersModule` (deuda conocida; ver `AGENTS.md`) |\n\n---\n\n## Licencia\n\nProyecto privado (`UNLICENSED`).\n";

/** README.md convertido a HTML (marked) para Swagger u otras vistas */
export const README_HTML: string = "<h1>Backend API — Jaramillo Mora (<code>auth</code>)</h1>\n<p>API REST con <strong>NestJS 11</strong>, <strong>Prisma 7</strong> (PostgreSQL) y <strong>Redis</strong>. Expone autenticación JWT y CRUD de usuarios, clientes y servicios.</p>\n<table>\n<thead>\n<tr>\n<th>Recurso</th>\n<th>Valor</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Prefijo API</td>\n<td><code>/api/v1/</code></td>\n</tr>\n<tr>\n<td>Swagger UI</td>\n<td><code>/api-docs</code></td>\n</tr>\n<tr>\n<td>Contexto para agentes IA</td>\n<td><a href=\"./AGENTS.md\"><code>AGENTS.md</code></a></td>\n</tr>\n<tr>\n<td>Skills del proyecto</td>\n<td><a href=\"./.agents/skills/\"><code>.agents/skills/</code></a></td>\n</tr>\n</tbody></table>\n<hr>\n<h2>Requisitos</h2>\n<ul>\n<li><strong>Node.js</strong> 24.x (alineado con la imagen Docker <code>node:24.14.1-alpine</code>)</li>\n<li><strong>npm</strong> (incluido con Node)</li>\n<li><strong>Docker</strong> + <strong>Docker Compose</strong> (infra y/o API en contenedor)</li>\n<li>PostgreSQL y Redis: vía Compose (recomendado) o instancias propias</li>\n</ul>\n<p>Opcional: crear de antemano las rutas de bind mounts de datos (<code>POSTGRES_DATA_PATH</code>, <code>REDIS_DATA_PATH</code>, <code>REDIS_LOGS_PATH</code>) con permisos de escritura.</p>\n<hr>\n<h2>Configuración de entorno</h2>\n<ol>\n<li>Copiar la plantilla:</li>\n</ol>\n<pre><code class=\"language-bash\">cp .env.example .env\n</code></pre>\n<ol start=\"2\">\n<li>Completar <code>.env</code> (nunca subir secretos al repositorio).</li>\n</ol>\n<h3>Variables principales</h3>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Descripción</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>SERVER_PORT</code></td>\n<td>Puerto HTTP de la API (local tipico <code>3001</code>; Compose mapea <code>3050</code>)</td>\n</tr>\n<tr>\n<td><code>HOST</code></td>\n<td>Host anunciado en logs (ej. <code>localhost</code>)</td>\n</tr>\n<tr>\n<td><code>DB_HOST</code>, <code>DB_PORT</code>, <code>DB_DATABASE</code>, <code>DB_USER</code>, <code>DB_PASSWORD</code></td>\n<td>Credenciales Postgres</td>\n</tr>\n<tr>\n<td><code>DATABASE_URL</code></td>\n<td>URL Prisma, ej. <code>postgres://USER:PASS@HOST:PORT/DB?schema=public</code></td>\n</tr>\n<tr>\n<td><code>POSTGRES_DATA_PATH</code></td>\n<td>Ruta absoluta en el host para el volumen de datos Postgres (Compose)</td>\n</tr>\n<tr>\n<td><code>DURATION_TOKEN</code></td>\n<td>Caducidad JWT (ej. <code>15d</code>)</td>\n</tr>\n<tr>\n<td><code>SECRECT_KEY</code></td>\n<td>Secreto JWT (<strong>nombre con typo histórico; no renombrar sin migrar envs</strong>)</td>\n</tr>\n<tr>\n<td><code>REDIS_HOST</code></td>\n<td><code>localhost</code> en desarrollo local; en Compose la API usa <code>redis-cache-jm</code></td>\n</tr>\n<tr>\n<td><code>REDIS_PORT</code></td>\n<td>Debe ser <strong>6390</strong> (coincide con <code>redis/redis.conf</code>)</td>\n</tr>\n<tr>\n<td><code>REDIS_PASSWORD</code></td>\n<td>Password Redis</td>\n</tr>\n<tr>\n<td><code>REDIS_DB</code>, <code>REDIS_SESSION_DB</code>, <code>REDIS_CACHE_DB</code></td>\n<td>Índices lógicos Redis</td>\n</tr>\n<tr>\n<td><code>REDIS_TTL</code>, <code>REDIS_MAX_ITEMS</code></td>\n<td>Caché Nest</td>\n</tr>\n<tr>\n<td><code>REDIS_DATA_PATH</code>, <code>REDIS_LOGS_PATH</code></td>\n<td>Bind mounts Redis</td>\n</tr>\n<tr>\n<td><code>REDIS_COMMANDER_PORT</code></td>\n<td>UI Redis Commander (profile <code>tools</code>)</td>\n</tr>\n<tr>\n<td><code>REDIS_MAXMEMORY</code>, <code>REDIS_MEMORY_*</code></td>\n<td>Límites de memoria Redis/Compose</td>\n</tr>\n</tbody></table>\n<p>El archivo tipado de lectura es <code>src/core/config/envs.ts</code>. Si añades una variable nueva, declárala ahí y en <code>.env.example</code>.</p>\n<blockquote>\n<p><strong>Nota:</strong> en este proyecto Redis escucha en <strong>6390</strong> (<code>redis/redis.conf</code>). El healthcheck de Compose debe usar ese puerto; <code>redis-cli</code> por defecto usa <code>6379</code> y marcaría el contenedor como <em>unhealthy</em>.</p>\n<p>Con <code>docker compose</code>, la API sobrescribe <code>DB_HOST</code>/<code>DATABASE_URL</code>/<code>REDIS_HOST</code>/<code>SERVER_PORT</code> para hablar con los servicios de la red interna (no uses <code>localhost</code> del host dentro del contenedor).</p>\n</blockquote>\n<hr>\n<h2>Arranque en desarrollo</h2>\n<h3>Opción A — Solo infra en Docker + API en el host (recomendada para coding)</h3>\n<pre><code class=\"language-bash\"># 1. Variables\ncp .env.example .env\n# Editar .env: DB_*, DATABASE_URL, REDIS_*, SECRECT_KEY, POSTGRES_DATA_PATH, etc.\n# Para API local: REDIS_HOST=localhost, REDIS_PORT=6390\n# DATABASE_URL apuntando a localhost:5453\n\n# 2. Postgres + Redis\ndocker compose up -d postgres-db-jm redis-cache-jm\n\n# 3. Dependencias y cliente Prisma\nnpm install\nnpx prisma generate\n\n# 4. API con watch\nnpm run start:dev\n</code></pre>\n<ul>\n<li>API: <code>http://localhost:&lt;SERVER_PORT&gt;</code> (según <code>.env</code>)</li>\n<li>Swagger: <code>http://localhost:&lt;SERVER_PORT&gt;/api-docs</code></li>\n<li>Postgres host: <code>localhost:5453</code></li>\n<li>Redis host: <code>localhost:6390</code></li>\n</ul>\n<h3>Opción B — Stack completo con Compose (API en contenedor)</h3>\n<pre><code class=\"language-bash\">cp .env.example .env\n# Ajustar paths de volúmenes y secretos\n# SERVER_PORT dentro del contenedor debe coincidir con el mapeo (Compose usa 3050:3050)\n\ndocker compose up -d --build\n</code></pre>\n<p>Servicios:</p>\n<table>\n<thead>\n<tr>\n<th>Servicio</th>\n<th>Contenedor</th>\n<th>Puerto host</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Postgres</td>\n<td><code>postgres-db-jm</code></td>\n<td><code>5453</code> → <code>5432</code></td>\n</tr>\n<tr>\n<td>Redis</td>\n<td><code>redis-cache-jm</code></td>\n<td><code>6390</code> → <code>6390</code></td>\n</tr>\n<tr>\n<td>API</td>\n<td><code>api-auth-jm</code></td>\n<td><code>3050</code> → <code>3050</code></td>\n</tr>\n</tbody></table>\n<p>El servicio API usa el stage Docker <strong><code>development</code></strong>, monta el código y ejecuta <code>start_docker.sh</code> (install, <code>prisma generate</code>, build, <strong>pm2</strong>).</p>\n<p>Redis Commander (opcional):</p>\n<pre><code class=\"language-bash\">docker compose --profile tools up -d redis-commander-jm\n</code></pre>\n<p>UI por defecto: <code>http://localhost:8091</code> (o <code>REDIS_COMMANDER_PORT</code>).</p>\n<h3>Scripts npm útiles</h3>\n<table>\n<thead>\n<tr>\n<th>Script</th>\n<th>Qué hace</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>npm run start:dev</code></td>\n<td>Limpia y arranca Nest en watch</td>\n</tr>\n<tr>\n<td><code>npm run start:debug</code></td>\n<td>Nest debug + watch</td>\n</tr>\n<tr>\n<td><code>npm run build</code></td>\n<td>Genera docs embebidas, limpia y compila (<code>nest build</code>)</td>\n</tr>\n<tr>\n<td><code>npm run start:prod</code></td>\n<td><code>node dist/main</code> (tras <code>build</code>)</td>\n</tr>\n<tr>\n<td><code>npm run prisma:generate:models</code></td>\n<td><code>prisma db pull</code> + <code>generate</code> (reflejar schema desde DB)</td>\n</tr>\n<tr>\n<td><code>npx prisma generate</code></td>\n<td>Regenerar cliente sin pull</td>\n</tr>\n<tr>\n<td><code>npm run lint</code></td>\n<td>ESLint con fix</td>\n</tr>\n<tr>\n<td><code>npm run test</code> / <code>test:e2e</code></td>\n<td>Jest unitario / e2e</td>\n</tr>\n<tr>\n<td><code>npm run docs:build</code></td>\n<td>Embed de este README en <code>src/core/docs/readme.generated.ts</code></td>\n</tr>\n</tbody></table>\n<p><code>postinstall</code> ejecuta <code>prisma generate</code> automáticamente.</p>\n<hr>\n<h2>Arranque en producción</h2>\n<h3>1. Build de la aplicación</h3>\n<pre><code class=\"language-bash\">npm ci\nnpx prisma generate\nnpm run build\nNODE_ENV=production npm run start:prod\n</code></pre>\n<p>Asegura que <code>DATABASE_URL</code>, <code>SECRECT_KEY</code>, Redis y <code>SERVER_PORT</code> estén definidos en el entorno de ejecución (no uses valores de desarrollo).</p>\n<blockquote>\n<p>El script <code>start:prod</code> apunta a <code>node dist/main</code>. Verifica en tu <code>dist/</code> la ruta real del entrypoint tras el build (<code>dist/main.js</code> vs <code>dist/src/main.js</code>) y alinea el script o el <code>CMD</code> Docker si difieren.</p>\n</blockquote>\n<h3>2. Imagen Docker multi-stage (API)</h3>\n<p>Dockerfile: <code>dockerfile/api/Dockerfile</code></p>\n<table>\n<thead>\n<tr>\n<th>Stage</th>\n<th>Uso</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>development</code></td>\n<td>Hot path / Compose actual</td>\n</tr>\n<tr>\n<td><code>builder</code></td>\n<td><code>npm run build</code> + deps de producción</td>\n</tr>\n<tr>\n<td><code>production</code></td>\n<td>Imagen final: <code>node dist/src/main.js</code>, <code>EXPOSE 3050</code></td>\n</tr>\n</tbody></table>\n<p>Ejemplo de build y ejecución del stage de producción:</p>\n<pre><code class=\"language-bash\">docker build -f dockerfile/api/Dockerfile --target production -t api-auth-jm:prod .\n\ndocker run --rm --env-file .env \\\n  -e REDIS_HOST=&lt;host-redis-accesible&gt; \\\n  -p 3050:3050 \\\n  api-auth-jm:prod\n</code></pre>\n<p>Postgres y Redis en producción deben ser servicios gestionados o contenedores con volúmenes persistentes, backups (<code>backup_db.sh</code> / carpeta <code>backups/</code>) y secretos fuera del código.</p>\n<h3>3. Compose orientado a producción</h3>\n<p>El <code>docker-compose.yml</code> actual está pensado sobre todo para <strong>desarrollo</strong> (<code>target: development</code>, bind mounts, pm2). Para producción:</p>\n<ol>\n<li>Construir/usar <code>--target production</code> en el servicio API.</li>\n<li>No montar el código fuente como volumen.</li>\n<li>Fijar <code>NODE_ENV=production</code> y secretos robustos.</li>\n<li>Revisar límites de memoria Redis y healthchecks.</li>\n<li>Exponer solo los puertos necesarios detrás de un reverse proxy (hay <code>vhost-auth.conf</code> como referencia).</li>\n</ol>\n<hr>\n<h2>Modelo de datos (Prisma)</h2>\n<p>Archivo: <code>prisma/schema.prisma</code> (schema SQL <code>public</code>).</p>\n<ul>\n<li><code>identity_type</code> → tipos de identificación</li>\n<li><code>users</code> → usuarios del sistema (login)</li>\n<li><code>clientes</code> → clientes de negocio</li>\n<li><code>servicios</code> / <code>tipo_servicios</code> → servicios contratados y catálogo</li>\n</ul>\n<p>Relaciones: tipos de id compartidos entre <code>users</code> y <code>clientes</code>; cada <code>servicio</code> pertenece a un <code>cliente</code> y a un <code>tipo_servicios</code>.</p>\n<p>Inicialización de DB en Docker: <code>init-scripts/init-db.sql</code> (montado en el entrypoint de Postgres).</p>\n<p>Si el esquema cambia en Postgres:</p>\n<pre><code class=\"language-bash\">npm run prisma:generate:models\n</code></pre>\n<hr>\n<h2>Desarrollar nuevos objetos (guía rápida)</h2>\n<p>Sigue el patrón de <strong><code>src/modules/clientes/</code></strong> (o <code>servicios/</code>). Contexto ampliado: <a href=\"./AGENTS.md\"><code>AGENTS.md</code></a>.</p>\n<h3>1. Crear el feature module</h3>\n<pre><code>src/modules/&lt;feature&gt;/\n  &lt;feature&gt;.module.ts\n  controllers/&lt;usecase&gt;/...\n  controllers/dto/int|out/\n  controllers/mappers/\n  services/&lt;usecase&gt;/\n  repositories/\n  interfaces/\n  entities/\n</code></pre>\n<h3>2. Capas</h3>\n<ol>\n<li><strong>Entity</strong> de dominio.</li>\n<li><strong>Interface</strong> abstracta del repositorio (token DI).</li>\n<li><strong>Prisma repository</strong> con <code>PrismaService</code>.</li>\n<li><strong>Service</strong> por caso de uso (crear, buscar, actualizar…).</li>\n<li><strong>DTOs</strong> de entrada/salida con <code>class-validator</code> y decoradores Swagger.</li>\n<li><strong>Mapper</strong> entity ↔ DTO.</li>\n<li><strong>Controller</strong>: delgado; <code>@UseGuards(JwtAuthGuard)</code> y <code>@ApiBearerAuth</code> si aplica.</li>\n</ol>\n<h3>3. Registrar</h3>\n<p>En <code>&lt;feature&gt;.module.ts</code>:</p>\n<pre><code class=\"language-typescript\">{ provide: MiRepository, useClass: PrismaMiRepository }\n</code></pre>\n<p>En <code>src/app.module.ts</code>, añadir el módulo a <code>imports</code>.</p>\n<h3>4. Auth y respuestas</h3>\n<ul>\n<li>Importar <code>AuthModule</code> desde <code>src/core/config/auth.module/</code>.</li>\n<li>Devolver datos; el <code>ResponseInterceptor</code> global arma la respuesta estándar.</li>\n<li>Opcional: <code>@ResponseMessage(&#39;...&#39;)</code>.</li>\n</ul>\n<h3>5. Caché (opcional)</h3>\n<p>Inyectar <code>RedisService</code> (<code>src/core/redis/</code>) — el módulo ya es global.</p>\n<h3>6. Calidad</h3>\n<pre><code class=\"language-bash\">npm run lint\nnpm run test\n</code></pre>\n<p>Convenciones Nest obligatorias del equipo: skill <strong>nestjs-best-practices</strong> en <code>.agents/skills/nestjs-best-practices/</code>.</p>\n<p>Commits (cuando se pidan): skills <strong>commiter</strong> + <strong>changelog-manager</strong> (Conventional Commits + <code>CHANGELOG.md</code>).</p>\n<hr>\n<h2>Módulos de negocio actuales</h2>\n<table>\n<thead>\n<tr>\n<th>Módulo</th>\n<th>Ruta</th>\n<th>Responsabilidad</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Users</td>\n<td><code>src/modules/users/</code></td>\n<td>Login JWT, consulta/actualización de usuarios</td>\n</tr>\n<tr>\n<td>Clientes</td>\n<td><code>src/modules/clientes/</code></td>\n<td>CRUD clientes y tipos de identificación</td>\n</tr>\n<tr>\n<td>Servicios</td>\n<td><code>src/modules/servicios/</code></td>\n<td>CRUD servicios, tipos y consultas por cliente</td>\n</tr>\n</tbody></table>\n<p>Infra transversal: <code>PrismaModule</code>, <code>RedisModule</code>, <code>AuthModule</code>, filter e interceptor globales.</p>\n<hr>\n<h2>Docker — resumen de archivos</h2>\n<table>\n<thead>\n<tr>\n<th>Archivo</th>\n<th>Rol</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>docker-compose.yml</code></td>\n<td>Orquestación Postgres, Redis, API, Commander</td>\n</tr>\n<tr>\n<td><code>dockerfile/api/Dockerfile</code></td>\n<td>Stages development / builder / production</td>\n</tr>\n<tr>\n<td><code>dockerfile/db/Dockerfile</code></td>\n<td>Imagen Postgres + init</td>\n</tr>\n<tr>\n<td><code>start_docker.sh</code></td>\n<td>Entrypoint contenedor API (dev)</td>\n</tr>\n<tr>\n<td><code>redis/redis.conf</code></td>\n<td>Redis en puerto 6390, persistencia RDB+AOF</td>\n</tr>\n</tbody></table>\n<p>Red Compose: <code>red_jaramillomora</code>.</p>\n<hr>\n<h2>Solución de problemas</h2>\n<table>\n<thead>\n<tr>\n<th>Problema</th>\n<th>Qué revisar</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>API no conecta a Redis</td>\n<td><code>REDIS_HOST</code> / <code>REDIS_PORT=6390</code> / password; en Compose el hostname es <code>redis-cache-jm</code></td>\n</tr>\n<tr>\n<td>Prisma no genera cliente</td>\n<td><code>DATABASE_URL</code> válida; <code>npx prisma generate</code></td>\n</tr>\n<tr>\n<td>Postgres no inicia</td>\n<td><code>POSTGRES_DATA_PATH</code> existe y es escribible; logs del contenedor</td>\n</tr>\n<tr>\n<td>JWT inválido</td>\n<td><code>SECRECT_KEY</code> y <code>DURATION_TOKEN</code> iguales en todos los entornos que firman/verifican</td>\n</tr>\n<tr>\n<td>Puerto ocupado</td>\n<td>Liberar <code>SERVER_PORT</code> / <code>3050</code> / <code>5453</code> / <code>6390</code></td>\n</tr>\n<tr>\n<td>Create user “no existe” en API</td>\n<td>El controller de creación no está registrado en <code>UsersModule</code> (deuda conocida; ver <code>AGENTS.md</code>)</td>\n</tr>\n</tbody></table>\n<hr>\n<h2>Licencia</h2>\n<p>Proyecto privado (<code>UNLICENSED</code>).</p>\n";
